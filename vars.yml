  vars_prompt:
    - name: "user"
      prompt: "username"
      private: no  
    - name: "password"
      prompt: "Enter password"
      private: yes

  # vars:
  #   cli:
  #     host: "{{ inventory_hostname }}"
  #     username: "{{user}}"
  #     password: "{{password}}"
  
